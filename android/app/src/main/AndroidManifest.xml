<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!--
      INTERNET is required by the WebView host even though this app makes no
      requests: Capacitor serves the bundled files over an https:// scheme so the
      WebView stays a secure context, which is what IndexedDB and the clipboard
      need. Nothing is fetched. There is no code here that can reach the network,
      which is what makes the claim checkable rather than a promise.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Lets a reminder land at the minute it is due rather than whenever Android
      next feels like waking up. It matters most at 3am, which is exactly when the
      phone is deepest in Doze and an inexact alarm can drift by hours.

      User-grantable, and the plugin degrades to an inexact alarm when it is not
      granted — so denying it makes reminders approximate, never broken. Deliberately
      *not* paired with USE_EXACT_ALARM, the auto-granted variant reserved for
      alarm-clock and calendar apps: a feed reminder is not an alarm clock, and
      claiming that permission to avoid asking would be the wrong trade.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>
